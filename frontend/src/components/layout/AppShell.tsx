import { BarChart3, Boxes, Coffee, FileText, LayoutDashboard, LogOut, ReceiptText, Settings, ShoppingCart, Users, WalletCards } from 'lucide-react';
import { NavLink, Outlet, useNavigate } from 'react-router-dom';
import { useAuth } from '../../features/auth/AuthProvider';

const navigation=[{label:'Dashboard',to:'/dashboard',icon:LayoutDashboard},{label:'POS',to:'/pos',icon:ShoppingCart},{label:'Cash Register',to:'/cash-register',icon:WalletCards},{label:'Bar Queue',to:'/bar',icon:Coffee},{label:'Menu & Products',to:'/products',icon:ReceiptText},{label:'Inventory',to:'/inventory',icon:Boxes},{label:'Finance',to:'/finance',icon:BarChart3},{label:'Invoices',to:'/invoices',icon:FileText},{label:'Staff',to:'/staff',icon:Users},{label:'Settings',to:'/settings',icon:Settings}];
export function AppShell(){const navigate=useNavigate();const {user,activeBusiness,activeLocation,selectBusiness,selectLocation,logout}=useAuth();return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">H</div><div><strong>Hospitality</strong><span>Bar & Café OS</span></div></div><nav className="nav-list" aria-label="Main navigation">{navigation.map(({label,to,icon:Icon})=><NavLink key={to} to={to} className={({isActive})=>`nav-item ${isActive?'active':''}`}><Icon size={19}/><span>{label}</span></NavLink>)}</nav><div className="sidebar-footer"><div className="location-chip"><span className="status-dot"/>{activeLocation?.name??'Choose location'}</div><small>{activeBusiness?.name??'Operational workspace'}</small></div></aside><main className="main-content"><header className="topbar"><div className="workspace-switchers"><label><span>Business</span><select value={activeBusiness?.id??''} onChange={e=>selectBusiness(e.target.value)}>{user?.businesses.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select></label><label><span>Location</span><select value={activeLocation?.id??''} onChange={e=>selectLocation(e.target.value)}>{activeBusiness?.locations.map(l=><option key={l.id} value={l.id}>{l.name}</option>)}</select></label></div><div className="topbar-actions"><button className="ghost-button" onClick={()=>navigate('/cash-register')}>Cash register</button><div className="account-chip"><div className="avatar">{user?.name.split(' ').map(x=>x[0]).slice(0,2).join('').toUpperCase()}</div><div><strong>{user?.name}</strong><span>{user?.email}</span></div></div><button className="icon-button" title="Sign out" onClick={()=>logout()}><LogOut size={18}/></button></div></header><div className="page-container"><Outlet/></div></main></div>}
